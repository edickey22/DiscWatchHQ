User-agent: *
Allow: /

# Disallow internal API paths from being listed in SERPs
# (they're crawlable for link-following but not useful as landing pages)
Disallow: /api/

# XML sitemap — generated at build time, served as a static file.
# Covers home, /games, /boutique, and all individual release pages.
Sitemap: /sitemap.xml
